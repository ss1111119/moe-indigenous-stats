## 1. 全體端取數

- [x] 1.1 交付 "Fetch general-student senior-secondary programme data per academic year"，依決策「一律走 CSV，只有 114 例外用 XLSX」：`fetch_senior.py` 取得 105–114 每學年的 `base3` 檔並快取於 `data/moe-senior/`，逐年印出學年與位元組數；快取齊全且無網路時仍能成功且不發任何請求。驗證：連線執行一次後斷網再執行一次，兩次都成功；`data/moe-senior/` 出現 10 個檔。
- [x] 1.2 交付單一學年失敗時的行為：任一學年取不到即中止並指名該學年，且不留下該學年的半截快取檔。驗證：把某一學年的網址改為無效值執行，確認訊息指名學年、該學年快取未產生。

## 2. 正規化

- [x] 2.1 交付 "Normalise the per-year format differences in one place"，依決策「跨年份的欄位與類別名稱以對照表集中處理」：把學程欄名的三種寫法（`等級名稱`／`學程名稱`／`學程(等級)名稱`）與類別名稱的差異（`進修部(學校)`／`進修部`、`專業群(職業)科`／`專業群科`）寫成明確對照表，集中在一處套用；正規化後若出現五類以外的類別即中止並列出未知類別與其學年。驗證：spec 中 "category normalisation" 的四列逐一核對；人工在某年檔案插入一個假類別後執行，確認中止並指名。
- [x] 2.4 依決策「編碼與分隔字元用嗅探決定，不寫死年份」：CSV 依序嘗試 UTF-8 with BOM 與 Big5，分隔字元由首行是否含 Tab 判定；兩種編碼都解不開即中止並指名學年。驗證：spec 中 "observed per-year formats" 的三列通過——110 為 UTF-8＋逗號、111 為 Big5＋Tab、112 為 UTF-8＋逗號，且三年解析出相同的欄位集合。
- [x] 2.2 交付 "Derive student counts by summing grade columns"，依決策「學生數一律由年級欄加總，不使用檔案自帶的學生數欄」：所有學年與兩種格式都以逐年級男女加延修生共 10 欄加總；某學年若兩種格式並存則驗證兩者總計相同，不同即中止並列出兩個數字。驗證：對 111–113（CSV 與 XLSX 並存的學年）確認兩種格式加總一致。
- [x] 2.3 交付 "Exclude the affiliated junior-high programme"：`附設國中部` 一律排除於高中職總計與占比之外。驗證：spec 中 "observed national total" 通過——114 學年扣除後五類合計為 532,256。

## 3. 對照輸出

- [x] 3.1 交付 "Produce the indigenous-versus-general comparison"，依決策「104 學年保留在原民端序列但不參與差距計算」：`build_stream.py` 增產 `out/senior_stream_compare.csv`，一列一個學年 × 分流，欄位為學年、分流、原民人數、原民占比、全體人數、全體占比、差距；涵蓋 104–114 共 55 列，104 的全體與差距欄留空。驗證：spec 中 "observed comparison" 的兩列相符（105 普通科 29.0／40.5／−11.6，114 普通科 38.0／51.7／−13.7）；104 列的全體欄為空。
- [x] 3.2 交付既有輸出不變：`out/senior_stream.csv` 的欄位與數值在本變更後完全不動。驗證：改動前後 `git diff` 對該檔為空。
- [x] 3.3 交付各年占比一致性與缺快取時的行為：任一有資料的學年、任一端的五類占比之和須為 100%，否則中止並指名學年；缺少 `out/senior_stream.csv` 時中止並指示先跑原民端建置。驗證：兩種情況各實測一次。
- [x] 3.4 交付 "Report the direction of the gap without explaining it"：建置完成時印出首尾可對照學年的普通科差距與其移動方向。驗證：spec 中 "observed direction" 通過——印出 105 的 −11.6 與 114 的 −13.7 並指出差距擴大。

## 4. 報告頁改寫

- [x] 4.1 交付 "Present streams as shares and disclose the absence of a comparison" 的改寫，依決策「主軸改為差距，原民端與全體端並列」：`export_report.py` 與 `geography_template.html` 讓分流區塊同時顯示兩端占比與差距，並以差距為主要數字；區塊位置維持在階梯之後、承接端之前。驗證：重跑報告後於瀏覽器確認三個區塊順序不變，且每個學年列都能看到原民占比、全體占比與差距。
- [x] 4.2 交付敘事修正：移除現有暗示原民端情況改善的措辭，改為「原民生普通科占比上升，但全體上升更快，差距由 11.6 擴大到 13.7 個百分點」；任何提及原民趨勢之處都同時給出同期全體趨勢。驗證：檢視區塊文字確認舊措辭已不存在，且每處原民趨勢敘述旁都有全體趨勢。
- [x] 4.3 交付缺對照年份的標示與不歸因說明：104 學年顯示原民數字並標明無對照；頁面明寫不解讀總人數變化、且不對差距擴大提出原因。驗證：確認 104 列有無對照標記；確認兩段不歸因文字存在。

## 5. 文件

- [x] 5.1 在 `README.md` 的快速開始與輸出表補上 `fetch_senior.py` 與 `out/senior_stream_compare.csv`，並**改寫**既有分流那一節——原本寫的「本輪沒有一般生對照」已不成立，改為呈現兩端與差距。驗證：依 README 指令從零跑一次，快取與輸出檔都產生成功。
- [x] 5.2 在該節記錄三件事：來源的跨年份格式差異（CSV／XLSX、無學生數欄、欄位與類別改名）、104 無對照、以及兩端同為教育部統計處故不構成交叉驗證。驗證：檢視該節確認三點齊備。
