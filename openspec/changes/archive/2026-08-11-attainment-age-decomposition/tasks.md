## 1. 年齡分解

- [x] 1.1 交付 "Decompose indigenous attainment by age band"：`build_attainment_age.py` 從既有 `out/adult_education.csv` 產出 `out/attainment_by_age.csv`，一列一個縣市 × 年齡組，欄位為資料時間、縣市代碼、縣市、年齡組、人數、專科以上人數、專科以上占比、人數過少。不抓取任何新資料。驗證：spec 中 "expected shape" 的範例通過——輸出 132 列（22 縣市 × 6 年齡組），人數加總為 490,336。
- [x] 1.2 交付上游缺檔與年齡組不符時的行為：`out/adult_education.csv` 不存在時中止並指示先執行 `build_education.py`；讀到的年齡組不是預期六組時中止並列出實際年齡組。兩種情況都不寫出任何輸出檔。驗證：暫時移走上游檔案執行，確認訊息正確且無輸出；再人工把某列年齡組改名後執行，確認中止並列出實際年齡組。
- [x] 1.3 交付 "Mark small cells as unstable and exclude them from range statistics"，依決策「人數過少的縣市其分年齡比率標記為不穩定」：人數低於 500 的格標記為「是」，且計算各年齡組的縣市全距時排除這些格。驗證：spec 中 "threshold behaviour" 的三組值通過（499 標記且不計、500 不標記且計入、4200 不標記且計入）。

## 2. 年齡標準化

- [x] 2.1 交付 "Produce age-standardised attainment by direct standardisation"，依決策「標準人口取全國原住民 15 歲以上人口的年齡結構」：以全國年齡結構為權重、直接法加權各縣市的組內占比，產出 `out/attainment_standardised.csv`，欄位為資料時間、縣市代碼、縣市、15歲以上人數、原始專科以上占比、年齡標準化占比、標準化差值、65歲以上占比、原始排名、標準化排名、排名變動。驗證：輸出 22 列；欄位名稱逐一核對。
- [x] 2.2 交付標準化的正確性檢查：全國層級的原始占比與標準化占比必須相等（以自身年齡結構標準化是恆等變換），不相等即表示權重算錯，程式須印出兩者供核對。驗證：執行後確認兩數字相等到小數點後兩位。
- [x] 2.3 依決策「同時輸出原始占比與標準化占比，不用後者取代前者」，確認實測值與提案一致：臺東縣原始 29.3%、標準化約 33.0%、65 歲以上占比 18.9%、排名由 22 升至中段；臺北市 53.9% → 52.3%。驗證：spec 中 "observed values at the 113-year-end reference date" 的三列數值逐一相符。
- [x] 2.4 交付各年齡組縣市全距的計算並印出：15-24 歲約 14.4 個百分點、35-44 歲約 30.6，皆排除人數過少的格。驗證：執行後檢視印出的六組全距，與提案表格相符。

- [x] 2.5 交付 "Mark counties whose standardised share rests on unstable cells"：計算各縣市落在不穩定格中的人口占比，達 50% 以上者標記，並驗證原始與標準化全距在排除被標記縣市後不變（若改變則改為排除後的數字對外呈現）。驗證：spec 中 "flagged counties at the 113-year-end reference date" 的三列相符（嘉義市 100% 標記、雲林縣 53% 標記、新竹市 19% 不標記）；兩種算法的全距皆為 24.6 與 21.4。

## 3. 報告頁

- [x] 3.1 交付 "Present crude and standardised shares together"：`export_report.py` 與 `geography_template.html` 讓存量區塊同時顯示原始與年齡標準化兩個占比，並各自標明回答的是哪一個問題。驗證：重跑報告後於瀏覽器確認兩個數字並列且各有標籤。
- [x] 3.2 依決策「明確標示本變更不推翻既有結論」，交付 "State the limits of standardisation"：頁面寫出標準化前後的全距（24.6 → 21.4 個百分點）、說明年齡結構只解釋約 13%，並說明年齡結構本身是遷移的結果故此為分解而非校正、不支持因果推論。驗證：確認三段文字存在，且全頁不出現「原本的說法錯了」這類措辭。
- [x] 3.3 交付既有結論不被改動：存量區塊的原始數字與散布圖的相關性結論在改動後維持原樣。驗證：改動前後比對頁面該兩處的文字與數值未變；`out/adult_education_summary.csv` 與 `out/geography.csv` 無差異。

## 4. 文件

- [x] 4.1 在 `README.md` 的輸出表與快速開始段補上 `build_attainment_age.py` 與兩個新輸出檔，並新增一節說明標準化方法（直接法、標準人口為全國原住民 15 歲以上年齡結構）、500 人門檻的理由，以及標準化不支持因果推論。驗證：依 README 指令從零跑一次，兩個輸出檔都產生成功。
- [x] 4.2 在該節記錄本變更驗證過但**未推翻**的兩件事：原始全距 24.6 個百分點、標準化後 21.4；散布圖相關性 +0.798 對 +0.806，去掉臺東後 +0.785 對 +0.802。驗證：檢視該節確認四個數字齊備且措辭為「仍然成立」而非「已更正」。
